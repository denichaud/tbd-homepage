apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tbd-homepage
spec:
  rules:
    - host: tbd.to
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tbd-homepage
                port:
                  number: 80
